# REPRO-ONLY build config (not part of the production taint chain):
# lets Jackson bind Lombok-generated constructors from JSON so the endpoint
# can be exercised manually by a reviewer. Source classes stay verbatim.
lombok.anyConstructor.addConstructorProperties = true
config.stopBubbling = true
