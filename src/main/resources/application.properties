# Intentionally minimal. This project contains NO credentials:
# the datasource is an embedded in-memory H2 instance auto-configured by Spring Boot
# (no spring.datasource.url / username / password is set anywhere).
spring.application.name=checkmarx-repro-a1-sql-injection
spring.sql.init.mode=always
